# OS
.DS_Store
Thumbs.db

# Archives
*.zip

# IDE
.idea/
.vscode/

# Local WordPress (if added later)
wordpress/
wp-config.php

# Deploy credentials (never commit)
.env
.env.deploy
deploy.local.json

# Deploy keys (never commit)
scripts/.deploy-keys/

# Local WordPress paths (never commit)
local.env
